const { redisClient } = require("../src/infrastructure")
const { DateTime } = require("luxon");
const { v4: uuid } = require('uuid');

async function test() {
    const list = []
    list.push({lst:"123"})
}

test()